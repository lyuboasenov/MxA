<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage
    x:Class="MxA.Views.TrainingPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:android="clr-namespace:Xamarin.Forms.PlatformConfiguration.AndroidSpecific;assembly=Xamarin.Forms.Core"
    xmlns:views="clr-namespace:MxA.Views"
    Title="{Binding Title}"
    android:TabbedPage.BarItemColor="{DynamicResource PageBackgroundColor}"
    android:TabbedPage.BarSelectedItemColor="{DynamicResource PrimaryTextColor}"
    android:TabbedPage.ToolbarPlacement="Bottom">

    <TabbedPage.ToolbarItems>
        <ToolbarItem
            Command="{Binding AddItemCommand}"
            Order="Secondary"
            Text="Add workout" />
        <ToolbarItem
            Command="{Binding AddItemCommand}"
            Order="Secondary"
            Text="Add exercise" />
        <ToolbarItem
            Command="{Binding ImportWorkoutsCommand}"
            Order="Secondary"
            Text="Import" />
    </TabbedPage.ToolbarItems>

    <!--  Pages can be added as references or inline  -->
    <views:WorkoutsPage Title="Workouts" />
    <views:ExercisesPage Title="Exercises" />
</TabbedPage>